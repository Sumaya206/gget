tasks:
  install-deps:
    name: Install dependencies
    description: Install Python project dependencies via pip
    command: |
      pip install -e ".[dev]" 2>/dev/null || pip install -r requirements.txt -r dev-requirements.txt
    triggeredBy:
      - postDevcontainerStart
